<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.StuMapper">
    <insert id="insert" parameterType="com.example.tilas.pojo.Student">
        insert into student(name, no, gender, phone, id_card, is_college, address, degree, graduation_date, clazz_id,
                            create_time, update_time)
        values (#{name}, #{no}, #{gender}, #{phone}, #{idCard}, #{isCollege}, #{address}, #{degree}, #{graduationDate},
                #{clazzId}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.example.tilas.pojo.Student">
        update student
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="no != null and no != ''">no = #{no},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="idCard != null and idCard != ''">id_card = #{idCard},</if>
            <if test="isCollege != null">is_college = #{isCollege},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="degree != null">degree = #{degree},</if>
            <if test="graduationDate != null">graduation_date = #{graduationDate},</if>
            <if test="clazzId != null">clazz_id = #{clazzId},</if>
            <if test="violationCount != null">violation_count = #{violationCount},</if>
            <if test="violationScore != null">violation_score = #{violationScore},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <update id="disciplinaryAction">
        update student set violation_score =#{score} where id=#{id}
    </update>

    <delete id="deleteByIds">
        delete from student where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>


    <select id="list" resultType="com.example.tilas.pojo.Student" parameterType="com.example.tilas.pojo.StuQueryParam">
        select stu.*, cla.name clazzName
        from student stu
        left join clazz cla on stu.clazz_id = cla.id
        <where>
            <if test="name != null and name != ''">
                stu.name like concat('%', #{name}, '%')
            </if>
            <if test="degree != null">
                and stu.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and stu.clazz_id = #{clazzId}
            </if>
        </where>
    </select>

    <select id="SearchById" resultType="com.example.tilas.pojo.Student">
        select id,
               name,
               no,
               gender,
               phone,
               id_card,
               is_college,
               address,
               degree,
               graduation_date,
               clazz_id,
               violation_count,
               violation_score,
               create_time,
               update_time
        from student
        where id = #{id}
    </select>

    <select id="countStuDegreeData" resultType="java.util.Map">
        select (case
            degree
                    when 1 then '初中'
                    when 2 then '高中'
                    when 3 then '大专'
                    when 4 then '本科'
                    when 5 then '硕士'
                    when 6 then '博士'
            end)        name,
               count(*) value
        from student
        group by degree
        order by value;
    </select>
    <select id="countStudentCountData" resultType="java.util.Map">
        select c.name clazz, count(s.id) number
        from clazz c
                 left join student s on c.id = s.clazz_id
        group by c.id, c.name
        order by c.id
    </select>
</mapper>

